# Upstox API (optional — enables Upstox as primary data source)
# Get your access token from: https://upstox.com/developer/api-documentation/
# Without this, the tool uses NSE scraping → Yahoo Finance fallback
UPSTOX_ACCESS_TOKEN=

# Slack notifications (optional)
SLACK_WEBHOOK=
